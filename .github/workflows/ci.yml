name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    uses: nocoo/ci/.github/workflows/bun-quality.yml@main
    with:
      bun-version: "1.3.5"
      test-command: "bun test src/__tests__/"
