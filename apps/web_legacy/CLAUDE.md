FROZEN — see docs/07-vite-web-migration-plan.md.

This workspace is the Next.js 16 + NextAuth + Google OAuth snapshot kept
read-only during the Vite + Cloudflare Worker migration. Do not add new
features here. Bug fixes only when the new `apps/web` + `apps/worker`
cannot yet take the change.
