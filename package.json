{
  "name": "backy",
  "version": "1.8.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun --cwd apps/web_legacy dev",
    "build": "bun --cwd apps/web_legacy build",
    "start": "bun --cwd apps/web_legacy start",
    "lint": "bun --cwd apps/web_legacy lint && bun --cwd apps/web lint && bun --cwd packages/api lint && bun --cwd apps/worker lint",
    "lint:staged": "bun --cwd apps/web_legacy lint:staged && bun --cwd apps/web lint:staged && bun --cwd packages/api lint:staged && bun --cwd apps/worker lint:staged",
    "typecheck": "bun --cwd apps/web_legacy typecheck && bun --cwd apps/web typecheck && bun --cwd packages/api typecheck && bun --cwd apps/worker typecheck && bun --cwd apps/cli typecheck",
    "test": "bun --cwd apps/web_legacy test && bun --cwd apps/web test && bun --cwd packages/api test && bun --cwd apps/worker test && bun --cwd apps/cli test",
    "test:coverage": "bun --cwd packages/api test:coverage && bun --cwd apps/worker test:coverage && bun --cwd apps/web_legacy test:coverage",
    "test:e2e": "bun --cwd apps/web_legacy test:e2e",
    "test:e2e:api": "bun --cwd apps/web_legacy test:e2e:api",
    "test:e2e:bdd": "bun --cwd apps/web_legacy test:e2e:bdd",
    "gate:security": "bun --cwd apps/web_legacy gate:security",
    "release": "bun --cwd apps/web_legacy release",
    "web:dev": "bun --cwd apps/web dev",
    "web:build": "bun --cwd apps/web build",
    "web:typecheck": "bun --cwd apps/web typecheck",
    "web:lint": "bun --cwd apps/web lint",
    "web:test": "bun --cwd apps/web test",
    "web:test:coverage": "bun --cwd apps/web test:coverage",
    "legacy:dev": "bun --cwd apps/web_legacy dev",
    "legacy:build": "bun --cwd apps/web_legacy build",
    "legacy:start": "bun --cwd apps/web_legacy start",
    "legacy:lint": "bun --cwd apps/web_legacy lint",
    "legacy:lint:staged": "bun --cwd apps/web_legacy lint:staged",
    "legacy:typecheck": "bun --cwd apps/web_legacy typecheck",
    "legacy:test": "bun --cwd apps/web_legacy test",
    "legacy:test:coverage": "bun --cwd apps/web_legacy test:coverage",
    "legacy:test:e2e": "bun --cwd apps/web_legacy test:e2e",
    "legacy:test:e2e:api": "bun --cwd apps/web_legacy test:e2e:api",
    "legacy:test:e2e:bdd": "bun --cwd apps/web_legacy test:e2e:bdd",
    "legacy:gate:security": "bun --cwd apps/web_legacy gate:security",
    "legacy:release": "bun --cwd apps/web_legacy release",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "overrides": {
    "minimatch": "^9.0.7",
    "flatted": "^3.4.0",
    "picomatch": "^4.0.4"
  }
}
